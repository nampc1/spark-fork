// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/ent/flowexecution"
	"github.com/lightsparkdev/spark/so/ent/schema/schematype"
)

// FlowExecution is the model entity for the FlowExecution schema.
type FlowExecution struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// The time when the entity was created.
	CreateTime time.Time `json:"create_time,omitempty"`
	// The time when the entity was last updated.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// COORDINATOR or PARTICIPANT. Determines the semantics of the decision_payload column.
	Role schematype.FlowExecutionRole `json:"role,omitempty"`
	// Mirrors gossip.ConsensusOperationType; identifies the flow this execution belongs to.
	OpType int32 `json:"op_type,omitempty"`
	// IN_FLIGHT until the role reaches a terminal decision (COMMITTED or ROLLED_BACK).
	Status schematype.FlowExecutionStatus `json:"status,omitempty"`
	// Index of the coordinator operator (matches SigningOperator.ID). Required; on coordinator rows this equals the coordinator's own self-index.
	CoordinatorIndex uint `json:"coordinator_index,omitempty"`
	// Marshalled google.protobuf.Any carrying the commit or rollback payload. Populated on coordinator rows once a decision is reached so ConsensusQueryOutcome can serve the payload.
	DecisionPayload *[]byte `json:"decision_payload,omitempty"`
	selectValues    sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FlowExecution) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case flowexecution.FieldDecisionPayload:
			values[i] = new([]byte)
		case flowexecution.FieldOpType, flowexecution.FieldCoordinatorIndex:
			values[i] = new(sql.NullInt64)
		case flowexecution.FieldRole, flowexecution.FieldStatus:
			values[i] = new(sql.NullString)
		case flowexecution.FieldCreateTime, flowexecution.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case flowexecution.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FlowExecution fields.
func (fe *FlowExecution) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case flowexecution.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				fe.ID = *value
			}
		case flowexecution.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				fe.CreateTime = value.Time
			}
		case flowexecution.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				fe.UpdateTime = value.Time
			}
		case flowexecution.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				fe.Role = schematype.FlowExecutionRole(value.String)
			}
		case flowexecution.FieldOpType:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field op_type", values[i])
			} else if value.Valid {
				fe.OpType = int32(value.Int64)
			}
		case flowexecution.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				fe.Status = schematype.FlowExecutionStatus(value.String)
			}
		case flowexecution.FieldCoordinatorIndex:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field coordinator_index", values[i])
			} else if value.Valid {
				fe.CoordinatorIndex = uint(value.Int64)
			}
		case flowexecution.FieldDecisionPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field decision_payload", values[i])
			} else if value != nil {
				fe.DecisionPayload = value
			}
		default:
			fe.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the FlowExecution.
// This includes values selected through modifiers, order, etc.
func (fe *FlowExecution) Value(name string) (ent.Value, error) {
	return fe.selectValues.Get(name)
}

// Update returns a builder for updating this FlowExecution.
// Note that you need to call FlowExecution.Unwrap() before calling this method if this FlowExecution
// was returned from a transaction, and the transaction was committed or rolled back.
func (fe *FlowExecution) Update() *FlowExecutionUpdateOne {
	return NewFlowExecutionClient(fe.config).UpdateOne(fe)
}

// Unwrap unwraps the FlowExecution entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (fe *FlowExecution) Unwrap() *FlowExecution {
	_tx, ok := fe.config.driver.(*txDriver)
	if !ok {
		panic("ent: FlowExecution is not a transactional entity")
	}
	fe.config.driver = _tx.drv
	return fe
}

// String implements the fmt.Stringer.
func (fe *FlowExecution) String() string {
	var builder strings.Builder
	builder.WriteString("FlowExecution(")
	builder.WriteString(fmt.Sprintf("id=%v, ", fe.ID))
	builder.WriteString("create_time=")
	builder.WriteString(fe.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(fe.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(fmt.Sprintf("%v", fe.Role))
	builder.WriteString(", ")
	builder.WriteString("op_type=")
	builder.WriteString(fmt.Sprintf("%v", fe.OpType))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", fe.Status))
	builder.WriteString(", ")
	builder.WriteString("coordinator_index=")
	builder.WriteString(fmt.Sprintf("%v", fe.CoordinatorIndex))
	builder.WriteString(", ")
	if v := fe.DecisionPayload; v != nil {
		builder.WriteString("decision_payload=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteByte(')')
	return builder.String()
}

// FlowExecutions is a parsable slice of FlowExecution.
type FlowExecutions []*FlowExecution
