// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/ent/partner"
	"github.com/lightsparkdev/spark/so/ent/schema/schematype"
	"github.com/lightsparkdev/spark/so/ent/transfer"
	"github.com/lightsparkdev/spark/so/ent/transferpartner"
)

// TransferPartnerCreate is the builder for creating a TransferPartner entity.
type TransferPartnerCreate struct {
	config
	mutation *TransferPartnerMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetCreateTime sets the "create_time" field.
func (tpc *TransferPartnerCreate) SetCreateTime(t time.Time) *TransferPartnerCreate {
	tpc.mutation.SetCreateTime(t)
	return tpc
}

// SetNillableCreateTime sets the "create_time" field if the given value is not nil.
func (tpc *TransferPartnerCreate) SetNillableCreateTime(t *time.Time) *TransferPartnerCreate {
	if t != nil {
		tpc.SetCreateTime(*t)
	}
	return tpc
}

// SetUpdateTime sets the "update_time" field.
func (tpc *TransferPartnerCreate) SetUpdateTime(t time.Time) *TransferPartnerCreate {
	tpc.mutation.SetUpdateTime(t)
	return tpc
}

// SetNillableUpdateTime sets the "update_time" field if the given value is not nil.
func (tpc *TransferPartnerCreate) SetNillableUpdateTime(t *time.Time) *TransferPartnerCreate {
	if t != nil {
		tpc.SetUpdateTime(*t)
	}
	return tpc
}

// SetType sets the "type" field.
func (tpc *TransferPartnerCreate) SetType(spt schematype.TransferPartnerType) *TransferPartnerCreate {
	tpc.mutation.SetType(spt)
	return tpc
}

// SetID sets the "id" field.
func (tpc *TransferPartnerCreate) SetID(u uuid.UUID) *TransferPartnerCreate {
	tpc.mutation.SetID(u)
	return tpc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (tpc *TransferPartnerCreate) SetNillableID(u *uuid.UUID) *TransferPartnerCreate {
	if u != nil {
		tpc.SetID(*u)
	}
	return tpc
}

// SetPartnerID sets the "partner" edge to the Partner entity by ID.
func (tpc *TransferPartnerCreate) SetPartnerID(id uuid.UUID) *TransferPartnerCreate {
	tpc.mutation.SetPartnerID(id)
	return tpc
}

// SetPartner sets the "partner" edge to the Partner entity.
func (tpc *TransferPartnerCreate) SetPartner(p *Partner) *TransferPartnerCreate {
	return tpc.SetPartnerID(p.ID)
}

// SetTransferID sets the "transfer" edge to the Transfer entity by ID.
func (tpc *TransferPartnerCreate) SetTransferID(id uuid.UUID) *TransferPartnerCreate {
	tpc.mutation.SetTransferID(id)
	return tpc
}

// SetTransfer sets the "transfer" edge to the Transfer entity.
func (tpc *TransferPartnerCreate) SetTransfer(t *Transfer) *TransferPartnerCreate {
	return tpc.SetTransferID(t.ID)
}

// Mutation returns the TransferPartnerMutation object of the builder.
func (tpc *TransferPartnerCreate) Mutation() *TransferPartnerMutation {
	return tpc.mutation
}

// Save creates the TransferPartner in the database.
func (tpc *TransferPartnerCreate) Save(ctx context.Context) (*TransferPartner, error) {
	tpc.defaults()
	return withHooks(ctx, tpc.sqlSave, tpc.mutation, tpc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (tpc *TransferPartnerCreate) SaveX(ctx context.Context) *TransferPartner {
	v, err := tpc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tpc *TransferPartnerCreate) Exec(ctx context.Context) error {
	_, err := tpc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tpc *TransferPartnerCreate) ExecX(ctx context.Context) {
	if err := tpc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (tpc *TransferPartnerCreate) defaults() {
	if _, ok := tpc.mutation.CreateTime(); !ok {
		v := transferpartner.DefaultCreateTime()
		tpc.mutation.SetCreateTime(v)
	}
	if _, ok := tpc.mutation.UpdateTime(); !ok {
		v := transferpartner.DefaultUpdateTime()
		tpc.mutation.SetUpdateTime(v)
	}
	if _, ok := tpc.mutation.ID(); !ok {
		v := transferpartner.DefaultID()
		tpc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tpc *TransferPartnerCreate) check() error {
	if _, ok := tpc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New(`ent: missing required field "TransferPartner.create_time"`)}
	}
	if _, ok := tpc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New(`ent: missing required field "TransferPartner.update_time"`)}
	}
	if _, ok := tpc.mutation.GetType(); !ok {
		return &ValidationError{Name: "type", err: errors.New(`ent: missing required field "TransferPartner.type"`)}
	}
	if v, ok := tpc.mutation.GetType(); ok {
		if err := transferpartner.TypeValidator(v); err != nil {
			return &ValidationError{Name: "type", err: fmt.Errorf(`ent: validator failed for field "TransferPartner.type": %w`, err)}
		}
	}
	if len(tpc.mutation.PartnerIDs()) == 0 {
		return &ValidationError{Name: "partner", err: errors.New(`ent: missing required edge "TransferPartner.partner"`)}
	}
	if len(tpc.mutation.TransferIDs()) == 0 {
		return &ValidationError{Name: "transfer", err: errors.New(`ent: missing required edge "TransferPartner.transfer"`)}
	}
	return nil
}

func (tpc *TransferPartnerCreate) sqlSave(ctx context.Context) (*TransferPartner, error) {
	if err := tpc.check(); err != nil {
		return nil, err
	}
	_node, _spec := tpc.createSpec()
	if err := sqlgraph.CreateNode(ctx, tpc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	tpc.mutation.id = &_node.ID
	tpc.mutation.done = true
	return _node, nil
}

func (tpc *TransferPartnerCreate) createSpec() (*TransferPartner, *sqlgraph.CreateSpec) {
	var (
		_node = &TransferPartner{config: tpc.config}
		_spec = sqlgraph.NewCreateSpec(transferpartner.Table, sqlgraph.NewFieldSpec(transferpartner.FieldID, field.TypeUUID))
	)
	_spec.OnConflict = tpc.conflict
	if id, ok := tpc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := tpc.mutation.CreateTime(); ok {
		_spec.SetField(transferpartner.FieldCreateTime, field.TypeTime, value)
		_node.CreateTime = value
	}
	if value, ok := tpc.mutation.UpdateTime(); ok {
		_spec.SetField(transferpartner.FieldUpdateTime, field.TypeTime, value)
		_node.UpdateTime = value
	}
	if value, ok := tpc.mutation.GetType(); ok {
		_spec.SetField(transferpartner.FieldType, field.TypeEnum, value)
		_node.Type = value
	}
	if nodes := tpc.mutation.PartnerIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   transferpartner.PartnerTable,
			Columns: []string{transferpartner.PartnerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(partner.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.transfer_partner_partner = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := tpc.mutation.TransferIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   transferpartner.TransferTable,
			Columns: []string{transferpartner.TransferColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(transfer.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.transfer_partner_transfer = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.TransferPartner.Create().
//		SetCreateTime(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.TransferPartnerUpsert) {
//			SetCreateTime(v+v).
//		}).
//		Exec(ctx)
func (tpc *TransferPartnerCreate) OnConflict(opts ...sql.ConflictOption) *TransferPartnerUpsertOne {
	tpc.conflict = opts
	return &TransferPartnerUpsertOne{
		create: tpc,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.TransferPartner.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (tpc *TransferPartnerCreate) OnConflictColumns(columns ...string) *TransferPartnerUpsertOne {
	tpc.conflict = append(tpc.conflict, sql.ConflictColumns(columns...))
	return &TransferPartnerUpsertOne{
		create: tpc,
	}
}

type (
	// TransferPartnerUpsertOne is the builder for "upsert"-ing
	//  one TransferPartner node.
	TransferPartnerUpsertOne struct {
		create *TransferPartnerCreate
	}

	// TransferPartnerUpsert is the "OnConflict" setter.
	TransferPartnerUpsert struct {
		*sql.UpdateSet
	}
)

// SetUpdateTime sets the "update_time" field.
func (u *TransferPartnerUpsert) SetUpdateTime(v time.Time) *TransferPartnerUpsert {
	u.Set(transferpartner.FieldUpdateTime, v)
	return u
}

// UpdateUpdateTime sets the "update_time" field to the value that was provided on create.
func (u *TransferPartnerUpsert) UpdateUpdateTime() *TransferPartnerUpsert {
	u.SetExcluded(transferpartner.FieldUpdateTime)
	return u
}

// UpdateNewValues updates the mutable fields using the new values that were set on create except the ID field.
// Using this option is equivalent to using:
//
//	client.TransferPartner.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(transferpartner.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *TransferPartnerUpsertOne) UpdateNewValues() *TransferPartnerUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		if _, exists := u.create.mutation.ID(); exists {
			s.SetIgnore(transferpartner.FieldID)
		}
		if _, exists := u.create.mutation.CreateTime(); exists {
			s.SetIgnore(transferpartner.FieldCreateTime)
		}
		if _, exists := u.create.mutation.GetType(); exists {
			s.SetIgnore(transferpartner.FieldType)
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.TransferPartner.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *TransferPartnerUpsertOne) Ignore() *TransferPartnerUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *TransferPartnerUpsertOne) DoNothing() *TransferPartnerUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the TransferPartnerCreate.OnConflict
// documentation for more info.
func (u *TransferPartnerUpsertOne) Update(set func(*TransferPartnerUpsert)) *TransferPartnerUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&TransferPartnerUpsert{UpdateSet: update})
	}))
	return u
}

// SetUpdateTime sets the "update_time" field.
func (u *TransferPartnerUpsertOne) SetUpdateTime(v time.Time) *TransferPartnerUpsertOne {
	return u.Update(func(s *TransferPartnerUpsert) {
		s.SetUpdateTime(v)
	})
}

// UpdateUpdateTime sets the "update_time" field to the value that was provided on create.
func (u *TransferPartnerUpsertOne) UpdateUpdateTime() *TransferPartnerUpsertOne {
	return u.Update(func(s *TransferPartnerUpsert) {
		s.UpdateUpdateTime()
	})
}

// Exec executes the query.
func (u *TransferPartnerUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for TransferPartnerCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *TransferPartnerUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *TransferPartnerUpsertOne) ID(ctx context.Context) (id uuid.UUID, err error) {
	if u.create.driver.Dialect() == dialect.MySQL {
		// In case of "ON CONFLICT", there is no way to get back non-numeric ID
		// fields from the database since MySQL does not support the RETURNING clause.
		return id, errors.New("ent: TransferPartnerUpsertOne.ID is not supported by MySQL driver. Use TransferPartnerUpsertOne.Exec instead")
	}
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *TransferPartnerUpsertOne) IDX(ctx context.Context) uuid.UUID {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// TransferPartnerCreateBulk is the builder for creating many TransferPartner entities in bulk.
type TransferPartnerCreateBulk struct {
	config
	err      error
	builders []*TransferPartnerCreate
	conflict []sql.ConflictOption
}

// Save creates the TransferPartner entities in the database.
func (tpcb *TransferPartnerCreateBulk) Save(ctx context.Context) ([]*TransferPartner, error) {
	if tpcb.err != nil {
		return nil, tpcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(tpcb.builders))
	nodes := make([]*TransferPartner, len(tpcb.builders))
	mutators := make([]Mutator, len(tpcb.builders))
	for i := range tpcb.builders {
		func(i int, root context.Context) {
			builder := tpcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TransferPartnerMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, tpcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = tpcb.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, tpcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, tpcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (tpcb *TransferPartnerCreateBulk) SaveX(ctx context.Context) []*TransferPartner {
	v, err := tpcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tpcb *TransferPartnerCreateBulk) Exec(ctx context.Context) error {
	_, err := tpcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tpcb *TransferPartnerCreateBulk) ExecX(ctx context.Context) {
	if err := tpcb.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.TransferPartner.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.TransferPartnerUpsert) {
//			SetCreateTime(v+v).
//		}).
//		Exec(ctx)
func (tpcb *TransferPartnerCreateBulk) OnConflict(opts ...sql.ConflictOption) *TransferPartnerUpsertBulk {
	tpcb.conflict = opts
	return &TransferPartnerUpsertBulk{
		create: tpcb,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.TransferPartner.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (tpcb *TransferPartnerCreateBulk) OnConflictColumns(columns ...string) *TransferPartnerUpsertBulk {
	tpcb.conflict = append(tpcb.conflict, sql.ConflictColumns(columns...))
	return &TransferPartnerUpsertBulk{
		create: tpcb,
	}
}

// TransferPartnerUpsertBulk is the builder for "upsert"-ing
// a bulk of TransferPartner nodes.
type TransferPartnerUpsertBulk struct {
	create *TransferPartnerCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.TransferPartner.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(transferpartner.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *TransferPartnerUpsertBulk) UpdateNewValues() *TransferPartnerUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		for _, b := range u.create.builders {
			if _, exists := b.mutation.ID(); exists {
				s.SetIgnore(transferpartner.FieldID)
			}
			if _, exists := b.mutation.CreateTime(); exists {
				s.SetIgnore(transferpartner.FieldCreateTime)
			}
			if _, exists := b.mutation.GetType(); exists {
				s.SetIgnore(transferpartner.FieldType)
			}
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.TransferPartner.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *TransferPartnerUpsertBulk) Ignore() *TransferPartnerUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *TransferPartnerUpsertBulk) DoNothing() *TransferPartnerUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the TransferPartnerCreateBulk.OnConflict
// documentation for more info.
func (u *TransferPartnerUpsertBulk) Update(set func(*TransferPartnerUpsert)) *TransferPartnerUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&TransferPartnerUpsert{UpdateSet: update})
	}))
	return u
}

// SetUpdateTime sets the "update_time" field.
func (u *TransferPartnerUpsertBulk) SetUpdateTime(v time.Time) *TransferPartnerUpsertBulk {
	return u.Update(func(s *TransferPartnerUpsert) {
		s.SetUpdateTime(v)
	})
}

// UpdateUpdateTime sets the "update_time" field to the value that was provided on create.
func (u *TransferPartnerUpsertBulk) UpdateUpdateTime() *TransferPartnerUpsertBulk {
	return u.Update(func(s *TransferPartnerUpsert) {
		s.UpdateUpdateTime()
	})
}

// Exec executes the query.
func (u *TransferPartnerUpsertBulk) Exec(ctx context.Context) error {
	if u.create.err != nil {
		return u.create.err
	}
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the TransferPartnerCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for TransferPartnerCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *TransferPartnerUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
