// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/ent/depositaddress"
	"github.com/lightsparkdev/spark/so/ent/depositaddresspartner"
	"github.com/lightsparkdev/spark/so/ent/partner"
	"github.com/lightsparkdev/spark/so/ent/predicate"
)

// DepositAddressPartnerQuery is the builder for querying DepositAddressPartner entities.
type DepositAddressPartnerQuery struct {
	config
	ctx                *QueryContext
	order              []depositaddresspartner.OrderOption
	inters             []Interceptor
	predicates         []predicate.DepositAddressPartner
	withPartner        *PartnerQuery
	withDepositAddress *DepositAddressQuery
	withFKs            bool
	modifiers          []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DepositAddressPartnerQuery builder.
func (dapq *DepositAddressPartnerQuery) Where(ps ...predicate.DepositAddressPartner) *DepositAddressPartnerQuery {
	dapq.predicates = append(dapq.predicates, ps...)
	return dapq
}

// Limit the number of records to be returned by this query.
func (dapq *DepositAddressPartnerQuery) Limit(limit int) *DepositAddressPartnerQuery {
	dapq.ctx.Limit = &limit
	return dapq
}

// Offset to start from.
func (dapq *DepositAddressPartnerQuery) Offset(offset int) *DepositAddressPartnerQuery {
	dapq.ctx.Offset = &offset
	return dapq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dapq *DepositAddressPartnerQuery) Unique(unique bool) *DepositAddressPartnerQuery {
	dapq.ctx.Unique = &unique
	return dapq
}

// Order specifies how the records should be ordered.
func (dapq *DepositAddressPartnerQuery) Order(o ...depositaddresspartner.OrderOption) *DepositAddressPartnerQuery {
	dapq.order = append(dapq.order, o...)
	return dapq
}

// QueryPartner chains the current query on the "partner" edge.
func (dapq *DepositAddressPartnerQuery) QueryPartner() *PartnerQuery {
	query := (&PartnerClient{config: dapq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dapq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dapq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(depositaddresspartner.Table, depositaddresspartner.FieldID, selector),
			sqlgraph.To(partner.Table, partner.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, depositaddresspartner.PartnerTable, depositaddresspartner.PartnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(dapq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDepositAddress chains the current query on the "deposit_address" edge.
func (dapq *DepositAddressPartnerQuery) QueryDepositAddress() *DepositAddressQuery {
	query := (&DepositAddressClient{config: dapq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dapq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dapq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(depositaddresspartner.Table, depositaddresspartner.FieldID, selector),
			sqlgraph.To(depositaddress.Table, depositaddress.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, depositaddresspartner.DepositAddressTable, depositaddresspartner.DepositAddressColumn),
		)
		fromU = sqlgraph.SetNeighbors(dapq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DepositAddressPartner entity from the query.
// Returns a *NotFoundError when no DepositAddressPartner was found.
func (dapq *DepositAddressPartnerQuery) First(ctx context.Context) (*DepositAddressPartner, error) {
	nodes, err := dapq.Limit(1).All(setContextOp(ctx, dapq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{depositaddresspartner.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dapq *DepositAddressPartnerQuery) FirstX(ctx context.Context) *DepositAddressPartner {
	node, err := dapq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DepositAddressPartner ID from the query.
// Returns a *NotFoundError when no DepositAddressPartner ID was found.
func (dapq *DepositAddressPartnerQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = dapq.Limit(1).IDs(setContextOp(ctx, dapq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{depositaddresspartner.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dapq *DepositAddressPartnerQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := dapq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DepositAddressPartner entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DepositAddressPartner entity is found.
// Returns a *NotFoundError when no DepositAddressPartner entities are found.
func (dapq *DepositAddressPartnerQuery) Only(ctx context.Context) (*DepositAddressPartner, error) {
	nodes, err := dapq.Limit(2).All(setContextOp(ctx, dapq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{depositaddresspartner.Label}
	default:
		return nil, &NotSingularError{depositaddresspartner.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dapq *DepositAddressPartnerQuery) OnlyX(ctx context.Context) *DepositAddressPartner {
	node, err := dapq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DepositAddressPartner ID in the query.
// Returns a *NotSingularError when more than one DepositAddressPartner ID is found.
// Returns a *NotFoundError when no entities are found.
func (dapq *DepositAddressPartnerQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = dapq.Limit(2).IDs(setContextOp(ctx, dapq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{depositaddresspartner.Label}
	default:
		err = &NotSingularError{depositaddresspartner.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dapq *DepositAddressPartnerQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := dapq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DepositAddressPartners.
func (dapq *DepositAddressPartnerQuery) All(ctx context.Context) ([]*DepositAddressPartner, error) {
	ctx = setContextOp(ctx, dapq.ctx, ent.OpQueryAll)
	if err := dapq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DepositAddressPartner, *DepositAddressPartnerQuery]()
	return withInterceptors[[]*DepositAddressPartner](ctx, dapq, qr, dapq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dapq *DepositAddressPartnerQuery) AllX(ctx context.Context) []*DepositAddressPartner {
	nodes, err := dapq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DepositAddressPartner IDs.
func (dapq *DepositAddressPartnerQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if dapq.ctx.Unique == nil && dapq.path != nil {
		dapq.Unique(true)
	}
	ctx = setContextOp(ctx, dapq.ctx, ent.OpQueryIDs)
	if err = dapq.Select(depositaddresspartner.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dapq *DepositAddressPartnerQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := dapq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dapq *DepositAddressPartnerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dapq.ctx, ent.OpQueryCount)
	if err := dapq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dapq, querierCount[*DepositAddressPartnerQuery](), dapq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dapq *DepositAddressPartnerQuery) CountX(ctx context.Context) int {
	count, err := dapq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dapq *DepositAddressPartnerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dapq.ctx, ent.OpQueryExist)
	switch _, err := dapq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dapq *DepositAddressPartnerQuery) ExistX(ctx context.Context) bool {
	exist, err := dapq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DepositAddressPartnerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dapq *DepositAddressPartnerQuery) Clone() *DepositAddressPartnerQuery {
	if dapq == nil {
		return nil
	}
	return &DepositAddressPartnerQuery{
		config:             dapq.config,
		ctx:                dapq.ctx.Clone(),
		order:              append([]depositaddresspartner.OrderOption{}, dapq.order...),
		inters:             append([]Interceptor{}, dapq.inters...),
		predicates:         append([]predicate.DepositAddressPartner{}, dapq.predicates...),
		withPartner:        dapq.withPartner.Clone(),
		withDepositAddress: dapq.withDepositAddress.Clone(),
		// clone intermediate query.
		sql:       dapq.sql.Clone(),
		path:      dapq.path,
		modifiers: append([]func(*sql.Selector){}, dapq.modifiers...),
	}
}

// WithPartner tells the query-builder to eager-load the nodes that are connected to
// the "partner" edge. The optional arguments are used to configure the query builder of the edge.
func (dapq *DepositAddressPartnerQuery) WithPartner(opts ...func(*PartnerQuery)) *DepositAddressPartnerQuery {
	query := (&PartnerClient{config: dapq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dapq.withPartner = query
	return dapq
}

// WithDepositAddress tells the query-builder to eager-load the nodes that are connected to
// the "deposit_address" edge. The optional arguments are used to configure the query builder of the edge.
func (dapq *DepositAddressPartnerQuery) WithDepositAddress(opts ...func(*DepositAddressQuery)) *DepositAddressPartnerQuery {
	query := (&DepositAddressClient{config: dapq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dapq.withDepositAddress = query
	return dapq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DepositAddressPartner.Query().
//		GroupBy(depositaddresspartner.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dapq *DepositAddressPartnerQuery) GroupBy(field string, fields ...string) *DepositAddressPartnerGroupBy {
	dapq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DepositAddressPartnerGroupBy{build: dapq}
	grbuild.flds = &dapq.ctx.Fields
	grbuild.label = depositaddresspartner.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.DepositAddressPartner.Query().
//		Select(depositaddresspartner.FieldCreateTime).
//		Scan(ctx, &v)
func (dapq *DepositAddressPartnerQuery) Select(fields ...string) *DepositAddressPartnerSelect {
	dapq.ctx.Fields = append(dapq.ctx.Fields, fields...)
	sbuild := &DepositAddressPartnerSelect{DepositAddressPartnerQuery: dapq}
	sbuild.label = depositaddresspartner.Label
	sbuild.flds, sbuild.scan = &dapq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DepositAddressPartnerSelect configured with the given aggregations.
func (dapq *DepositAddressPartnerQuery) Aggregate(fns ...AggregateFunc) *DepositAddressPartnerSelect {
	return dapq.Select().Aggregate(fns...)
}

func (dapq *DepositAddressPartnerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dapq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dapq); err != nil {
				return err
			}
		}
	}
	for _, f := range dapq.ctx.Fields {
		if !depositaddresspartner.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dapq.path != nil {
		prev, err := dapq.path(ctx)
		if err != nil {
			return err
		}
		dapq.sql = prev
	}
	return nil
}

func (dapq *DepositAddressPartnerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DepositAddressPartner, error) {
	var (
		nodes       = []*DepositAddressPartner{}
		withFKs     = dapq.withFKs
		_spec       = dapq.querySpec()
		loadedTypes = [2]bool{
			dapq.withPartner != nil,
			dapq.withDepositAddress != nil,
		}
	)
	if dapq.withPartner != nil || dapq.withDepositAddress != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, depositaddresspartner.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DepositAddressPartner).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DepositAddressPartner{config: dapq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(dapq.modifiers) > 0 {
		_spec.Modifiers = dapq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dapq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dapq.withPartner; query != nil {
		if err := dapq.loadPartner(ctx, query, nodes, nil,
			func(n *DepositAddressPartner, e *Partner) { n.Edges.Partner = e }); err != nil {
			return nil, err
		}
	}
	if query := dapq.withDepositAddress; query != nil {
		if err := dapq.loadDepositAddress(ctx, query, nodes, nil,
			func(n *DepositAddressPartner, e *DepositAddress) { n.Edges.DepositAddress = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dapq *DepositAddressPartnerQuery) loadPartner(ctx context.Context, query *PartnerQuery, nodes []*DepositAddressPartner, init func(*DepositAddressPartner), assign func(*DepositAddressPartner, *Partner)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DepositAddressPartner)
	for i := range nodes {
		if nodes[i].deposit_address_partner_partner == nil {
			continue
		}
		fk := *nodes[i].deposit_address_partner_partner
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(partner.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "deposit_address_partner_partner" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (dapq *DepositAddressPartnerQuery) loadDepositAddress(ctx context.Context, query *DepositAddressQuery, nodes []*DepositAddressPartner, init func(*DepositAddressPartner), assign func(*DepositAddressPartner, *DepositAddress)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DepositAddressPartner)
	for i := range nodes {
		if nodes[i].deposit_address_partner_deposit_address == nil {
			continue
		}
		fk := *nodes[i].deposit_address_partner_deposit_address
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(depositaddress.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "deposit_address_partner_deposit_address" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (dapq *DepositAddressPartnerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dapq.querySpec()
	if len(dapq.modifiers) > 0 {
		_spec.Modifiers = dapq.modifiers
	}
	_spec.Node.Columns = dapq.ctx.Fields
	if len(dapq.ctx.Fields) > 0 {
		_spec.Unique = dapq.ctx.Unique != nil && *dapq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dapq.driver, _spec)
}

func (dapq *DepositAddressPartnerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(depositaddresspartner.Table, depositaddresspartner.Columns, sqlgraph.NewFieldSpec(depositaddresspartner.FieldID, field.TypeUUID))
	_spec.From = dapq.sql
	if unique := dapq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dapq.path != nil {
		_spec.Unique = true
	}
	if fields := dapq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, depositaddresspartner.FieldID)
		for i := range fields {
			if fields[i] != depositaddresspartner.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dapq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dapq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dapq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dapq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dapq *DepositAddressPartnerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dapq.driver.Dialect())
	t1 := builder.Table(depositaddresspartner.Table)
	columns := dapq.ctx.Fields
	if len(columns) == 0 {
		columns = depositaddresspartner.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dapq.sql != nil {
		selector = dapq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dapq.ctx.Unique != nil && *dapq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range dapq.modifiers {
		m(selector)
	}
	for _, p := range dapq.predicates {
		p(selector)
	}
	for _, p := range dapq.order {
		p(selector)
	}
	if offset := dapq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dapq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (dapq *DepositAddressPartnerQuery) ForUpdate(opts ...sql.LockOption) *DepositAddressPartnerQuery {
	if dapq.driver.Dialect() == dialect.Postgres {
		dapq.Unique(false)
	}
	dapq.modifiers = append(dapq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return dapq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (dapq *DepositAddressPartnerQuery) ForShare(opts ...sql.LockOption) *DepositAddressPartnerQuery {
	if dapq.driver.Dialect() == dialect.Postgres {
		dapq.Unique(false)
	}
	dapq.modifiers = append(dapq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return dapq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dapq *DepositAddressPartnerQuery) Modify(modifiers ...func(s *sql.Selector)) *DepositAddressPartnerSelect {
	dapq.modifiers = append(dapq.modifiers, modifiers...)
	return dapq.Select()
}

// DepositAddressPartnerGroupBy is the group-by builder for DepositAddressPartner entities.
type DepositAddressPartnerGroupBy struct {
	selector
	build *DepositAddressPartnerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dapgb *DepositAddressPartnerGroupBy) Aggregate(fns ...AggregateFunc) *DepositAddressPartnerGroupBy {
	dapgb.fns = append(dapgb.fns, fns...)
	return dapgb
}

// Scan applies the selector query and scans the result into the given value.
func (dapgb *DepositAddressPartnerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dapgb.build.ctx, ent.OpQueryGroupBy)
	if err := dapgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DepositAddressPartnerQuery, *DepositAddressPartnerGroupBy](ctx, dapgb.build, dapgb, dapgb.build.inters, v)
}

func (dapgb *DepositAddressPartnerGroupBy) sqlScan(ctx context.Context, root *DepositAddressPartnerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dapgb.fns))
	for _, fn := range dapgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dapgb.flds)+len(dapgb.fns))
		for _, f := range *dapgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dapgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dapgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DepositAddressPartnerSelect is the builder for selecting fields of DepositAddressPartner entities.
type DepositAddressPartnerSelect struct {
	*DepositAddressPartnerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (daps *DepositAddressPartnerSelect) Aggregate(fns ...AggregateFunc) *DepositAddressPartnerSelect {
	daps.fns = append(daps.fns, fns...)
	return daps
}

// Scan applies the selector query and scans the result into the given value.
func (daps *DepositAddressPartnerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, daps.ctx, ent.OpQuerySelect)
	if err := daps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DepositAddressPartnerQuery, *DepositAddressPartnerSelect](ctx, daps.DepositAddressPartnerQuery, daps, daps.inters, v)
}

func (daps *DepositAddressPartnerSelect) sqlScan(ctx context.Context, root *DepositAddressPartnerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(daps.fns))
	for _, fn := range daps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*daps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := daps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (daps *DepositAddressPartnerSelect) Modify(modifiers ...func(s *sql.Selector)) *DepositAddressPartnerSelect {
	daps.modifiers = append(daps.modifiers, modifiers...)
	return daps
}
