// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/common/keys"
	"github.com/lightsparkdev/spark/so/ent/schema/schematype"
	"github.com/lightsparkdev/spark/so/ent/transfer"
	"github.com/lightsparkdev/spark/so/ent/transferreceiver"
)

// TransferReceiver is the model entity for the TransferReceiver schema.
type TransferReceiver struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// The time when the entity was created.
	CreateTime time.Time `json:"create_time,omitempty"`
	// The time when the entity was last updated.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Foreign key to Transfer
	TransferID uuid.UUID `json:"transfer_id,omitempty"`
	// The identity public key of this receiver of the transfer.
	IdentityPubkey keys.Public `json:"identity_pubkey,omitempty"`
	// Current state of this receiver in the claim process (e.g. INITIATED, RECEIVER_CLAIM_PENDING, RECEIVER_KEY_TWEAKED, COMPLETED)
	Status schematype.TransferReceiverStatus `json:"status,omitempty"`
	// The time when the transfer claim was completed.
	CompletionTime time.Time `json:"completion_time,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TransferReceiverQuery when eager-loading is set.
	Edges        TransferReceiverEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TransferReceiverEdges holds the relations/edges for other nodes in the graph.
type TransferReceiverEdges struct {
	// Transfer holds the value of the transfer edge.
	Transfer *Transfer `json:"transfer,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TransferOrErr returns the Transfer value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TransferReceiverEdges) TransferOrErr() (*Transfer, error) {
	if e.Transfer != nil {
		return e.Transfer, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: transfer.Label}
	}
	return nil, &NotLoadedError{edge: "transfer"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TransferReceiver) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case transferreceiver.FieldIdentityPubkey:
			values[i] = new(keys.Public)
		case transferreceiver.FieldStatus:
			values[i] = new(sql.NullString)
		case transferreceiver.FieldCreateTime, transferreceiver.FieldUpdateTime, transferreceiver.FieldCompletionTime:
			values[i] = new(sql.NullTime)
		case transferreceiver.FieldID, transferreceiver.FieldTransferID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TransferReceiver fields.
func (tr *TransferReceiver) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case transferreceiver.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				tr.ID = *value
			}
		case transferreceiver.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				tr.CreateTime = value.Time
			}
		case transferreceiver.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				tr.UpdateTime = value.Time
			}
		case transferreceiver.FieldTransferID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field transfer_id", values[i])
			} else if value != nil {
				tr.TransferID = *value
			}
		case transferreceiver.FieldIdentityPubkey:
			if value, ok := values[i].(*keys.Public); !ok {
				return fmt.Errorf("unexpected type %T for field identity_pubkey", values[i])
			} else if value != nil {
				tr.IdentityPubkey = *value
			}
		case transferreceiver.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				tr.Status = schematype.TransferReceiverStatus(value.String)
			}
		case transferreceiver.FieldCompletionTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field completion_time", values[i])
			} else if value.Valid {
				tr.CompletionTime = value.Time
			}
		default:
			tr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TransferReceiver.
// This includes values selected through modifiers, order, etc.
func (tr *TransferReceiver) Value(name string) (ent.Value, error) {
	return tr.selectValues.Get(name)
}

// QueryTransfer queries the "transfer" edge of the TransferReceiver entity.
func (tr *TransferReceiver) QueryTransfer() *TransferQuery {
	return NewTransferReceiverClient(tr.config).QueryTransfer(tr)
}

// Update returns a builder for updating this TransferReceiver.
// Note that you need to call TransferReceiver.Unwrap() before calling this method if this TransferReceiver
// was returned from a transaction, and the transaction was committed or rolled back.
func (tr *TransferReceiver) Update() *TransferReceiverUpdateOne {
	return NewTransferReceiverClient(tr.config).UpdateOne(tr)
}

// Unwrap unwraps the TransferReceiver entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tr *TransferReceiver) Unwrap() *TransferReceiver {
	_tx, ok := tr.config.driver.(*txDriver)
	if !ok {
		panic("ent: TransferReceiver is not a transactional entity")
	}
	tr.config.driver = _tx.drv
	return tr
}

// String implements the fmt.Stringer.
func (tr *TransferReceiver) String() string {
	var builder strings.Builder
	builder.WriteString("TransferReceiver(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tr.ID))
	builder.WriteString("create_time=")
	builder.WriteString(tr.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(tr.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("transfer_id=")
	builder.WriteString(fmt.Sprintf("%v", tr.TransferID))
	builder.WriteString(", ")
	builder.WriteString("identity_pubkey=")
	builder.WriteString(fmt.Sprintf("%v", tr.IdentityPubkey))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", tr.Status))
	builder.WriteString(", ")
	builder.WriteString("completion_time=")
	builder.WriteString(tr.CompletionTime.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TransferReceivers is a parsable slice of TransferReceiver.
type TransferReceivers []*TransferReceiver
