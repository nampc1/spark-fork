// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/ent/partner"
	"github.com/lightsparkdev/spark/so/ent/predicate"
	"github.com/lightsparkdev/spark/so/ent/transferpartner"
)

// TransferPartnerUpdate is the builder for updating TransferPartner entities.
type TransferPartnerUpdate struct {
	config
	hooks     []Hook
	mutation  *TransferPartnerMutation
	modifiers []func(*sql.UpdateBuilder)
}

// Where appends a list predicates to the TransferPartnerUpdate builder.
func (tpu *TransferPartnerUpdate) Where(ps ...predicate.TransferPartner) *TransferPartnerUpdate {
	tpu.mutation.Where(ps...)
	return tpu
}

// SetUpdateTime sets the "update_time" field.
func (tpu *TransferPartnerUpdate) SetUpdateTime(t time.Time) *TransferPartnerUpdate {
	tpu.mutation.SetUpdateTime(t)
	return tpu
}

// SetPartnerID sets the "partner" edge to the Partner entity by ID.
func (tpu *TransferPartnerUpdate) SetPartnerID(id uuid.UUID) *TransferPartnerUpdate {
	tpu.mutation.SetPartnerID(id)
	return tpu
}

// SetPartner sets the "partner" edge to the Partner entity.
func (tpu *TransferPartnerUpdate) SetPartner(p *Partner) *TransferPartnerUpdate {
	return tpu.SetPartnerID(p.ID)
}

// Mutation returns the TransferPartnerMutation object of the builder.
func (tpu *TransferPartnerUpdate) Mutation() *TransferPartnerMutation {
	return tpu.mutation
}

// ClearPartner clears the "partner" edge to the Partner entity.
func (tpu *TransferPartnerUpdate) ClearPartner() *TransferPartnerUpdate {
	tpu.mutation.ClearPartner()
	return tpu
}

// Save executes the query and returns the number of nodes affected by the update operation.
func (tpu *TransferPartnerUpdate) Save(ctx context.Context) (int, error) {
	tpu.defaults()
	return withHooks(ctx, tpu.sqlSave, tpu.mutation, tpu.hooks)
}

// SaveX is like Save, but panics if an error occurs.
func (tpu *TransferPartnerUpdate) SaveX(ctx context.Context) int {
	affected, err := tpu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (tpu *TransferPartnerUpdate) Exec(ctx context.Context) error {
	_, err := tpu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tpu *TransferPartnerUpdate) ExecX(ctx context.Context) {
	if err := tpu.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (tpu *TransferPartnerUpdate) defaults() {
	if _, ok := tpu.mutation.UpdateTime(); !ok {
		v := transferpartner.UpdateDefaultUpdateTime()
		tpu.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tpu *TransferPartnerUpdate) check() error {
	if tpu.mutation.PartnerCleared() && len(tpu.mutation.PartnerIDs()) > 0 {
		return errors.New(`ent: clearing a required unique edge "TransferPartner.partner"`)
	}
	if tpu.mutation.TransferCleared() && len(tpu.mutation.TransferIDs()) > 0 {
		return errors.New(`ent: clearing a required unique edge "TransferPartner.transfer"`)
	}
	return nil
}

// Modify adds a statement modifier for attaching custom logic to the UPDATE statement.
func (tpu *TransferPartnerUpdate) Modify(modifiers ...func(u *sql.UpdateBuilder)) *TransferPartnerUpdate {
	tpu.modifiers = append(tpu.modifiers, modifiers...)
	return tpu
}

func (tpu *TransferPartnerUpdate) sqlSave(ctx context.Context) (n int, err error) {
	if err := tpu.check(); err != nil {
		return n, err
	}
	_spec := sqlgraph.NewUpdateSpec(transferpartner.Table, transferpartner.Columns, sqlgraph.NewFieldSpec(transferpartner.FieldID, field.TypeUUID))
	if ps := tpu.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := tpu.mutation.UpdateTime(); ok {
		_spec.SetField(transferpartner.FieldUpdateTime, field.TypeTime, value)
	}
	if tpu.mutation.PartnerCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   transferpartner.PartnerTable,
			Columns: []string{transferpartner.PartnerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(partner.FieldID, field.TypeUUID),
			},
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := tpu.mutation.PartnerIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   transferpartner.PartnerTable,
			Columns: []string{transferpartner.PartnerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(partner.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	_spec.AddModifiers(tpu.modifiers...)
	if n, err = sqlgraph.UpdateNodes(ctx, tpu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{transferpartner.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return 0, err
	}
	tpu.mutation.done = true
	return n, nil
}

// TransferPartnerUpdateOne is the builder for updating a single TransferPartner entity.
type TransferPartnerUpdateOne struct {
	config
	fields    []string
	hooks     []Hook
	mutation  *TransferPartnerMutation
	modifiers []func(*sql.UpdateBuilder)
}

// SetUpdateTime sets the "update_time" field.
func (tpuo *TransferPartnerUpdateOne) SetUpdateTime(t time.Time) *TransferPartnerUpdateOne {
	tpuo.mutation.SetUpdateTime(t)
	return tpuo
}

// SetPartnerID sets the "partner" edge to the Partner entity by ID.
func (tpuo *TransferPartnerUpdateOne) SetPartnerID(id uuid.UUID) *TransferPartnerUpdateOne {
	tpuo.mutation.SetPartnerID(id)
	return tpuo
}

// SetPartner sets the "partner" edge to the Partner entity.
func (tpuo *TransferPartnerUpdateOne) SetPartner(p *Partner) *TransferPartnerUpdateOne {
	return tpuo.SetPartnerID(p.ID)
}

// Mutation returns the TransferPartnerMutation object of the builder.
func (tpuo *TransferPartnerUpdateOne) Mutation() *TransferPartnerMutation {
	return tpuo.mutation
}

// ClearPartner clears the "partner" edge to the Partner entity.
func (tpuo *TransferPartnerUpdateOne) ClearPartner() *TransferPartnerUpdateOne {
	tpuo.mutation.ClearPartner()
	return tpuo
}

// Where appends a list predicates to the TransferPartnerUpdate builder.
func (tpuo *TransferPartnerUpdateOne) Where(ps ...predicate.TransferPartner) *TransferPartnerUpdateOne {
	tpuo.mutation.Where(ps...)
	return tpuo
}

// Select allows selecting one or more fields (columns) of the returned entity.
// The default is selecting all fields defined in the entity schema.
func (tpuo *TransferPartnerUpdateOne) Select(field string, fields ...string) *TransferPartnerUpdateOne {
	tpuo.fields = append([]string{field}, fields...)
	return tpuo
}

// Save executes the query and returns the updated TransferPartner entity.
func (tpuo *TransferPartnerUpdateOne) Save(ctx context.Context) (*TransferPartner, error) {
	tpuo.defaults()
	return withHooks(ctx, tpuo.sqlSave, tpuo.mutation, tpuo.hooks)
}

// SaveX is like Save, but panics if an error occurs.
func (tpuo *TransferPartnerUpdateOne) SaveX(ctx context.Context) *TransferPartner {
	node, err := tpuo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// Exec executes the query on the entity.
func (tpuo *TransferPartnerUpdateOne) Exec(ctx context.Context) error {
	_, err := tpuo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tpuo *TransferPartnerUpdateOne) ExecX(ctx context.Context) {
	if err := tpuo.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (tpuo *TransferPartnerUpdateOne) defaults() {
	if _, ok := tpuo.mutation.UpdateTime(); !ok {
		v := transferpartner.UpdateDefaultUpdateTime()
		tpuo.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tpuo *TransferPartnerUpdateOne) check() error {
	if tpuo.mutation.PartnerCleared() && len(tpuo.mutation.PartnerIDs()) > 0 {
		return errors.New(`ent: clearing a required unique edge "TransferPartner.partner"`)
	}
	if tpuo.mutation.TransferCleared() && len(tpuo.mutation.TransferIDs()) > 0 {
		return errors.New(`ent: clearing a required unique edge "TransferPartner.transfer"`)
	}
	return nil
}

// Modify adds a statement modifier for attaching custom logic to the UPDATE statement.
func (tpuo *TransferPartnerUpdateOne) Modify(modifiers ...func(u *sql.UpdateBuilder)) *TransferPartnerUpdateOne {
	tpuo.modifiers = append(tpuo.modifiers, modifiers...)
	return tpuo
}

func (tpuo *TransferPartnerUpdateOne) sqlSave(ctx context.Context) (_node *TransferPartner, err error) {
	if err := tpuo.check(); err != nil {
		return _node, err
	}
	_spec := sqlgraph.NewUpdateSpec(transferpartner.Table, transferpartner.Columns, sqlgraph.NewFieldSpec(transferpartner.FieldID, field.TypeUUID))
	id, ok := tpuo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "id", err: errors.New(`ent: missing "TransferPartner.id" for update`)}
	}
	_spec.Node.ID.Value = id
	if fields := tpuo.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, transferpartner.FieldID)
		for _, f := range fields {
			if !transferpartner.ValidColumn(f) {
				return nil, &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
			}
			if f != transferpartner.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, f)
			}
		}
	}
	if ps := tpuo.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := tpuo.mutation.UpdateTime(); ok {
		_spec.SetField(transferpartner.FieldUpdateTime, field.TypeTime, value)
	}
	if tpuo.mutation.PartnerCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   transferpartner.PartnerTable,
			Columns: []string{transferpartner.PartnerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(partner.FieldID, field.TypeUUID),
			},
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := tpuo.mutation.PartnerIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   transferpartner.PartnerTable,
			Columns: []string{transferpartner.PartnerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(partner.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	_spec.AddModifiers(tpuo.modifiers...)
	_node = &TransferPartner{config: tpuo.config}
	_spec.Assign = _node.assignValues
	_spec.ScanValues = _node.scanValues
	if err = sqlgraph.UpdateNode(ctx, tpuo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{transferpartner.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	tpuo.mutation.done = true
	return _node, nil
}
