// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/common/keys"
	"github.com/lightsparkdev/spark/so/ent/schema/schematype"
	"github.com/lightsparkdev/spark/so/ent/transfer"
	"github.com/lightsparkdev/spark/so/ent/transfersender"
)

// TransferSender is the model entity for the TransferSender schema.
type TransferSender struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// The time when the entity was created.
	CreateTime time.Time `json:"create_time,omitempty"`
	// The time when the entity was last updated.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Foreign key to Transfer
	TransferID uuid.UUID `json:"transfer_id,omitempty"`
	// The identity public key of this sender of the transfer.
	IdentityPubkey keys.Public `json:"identity_pubkey,omitempty"`
	// Denormalized from transfers.type.
	TransferType schematype.TransferType `json:"transfer_type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TransferSenderQuery when eager-loading is set.
	Edges        TransferSenderEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TransferSenderEdges holds the relations/edges for other nodes in the graph.
type TransferSenderEdges struct {
	// Transfer holds the value of the transfer edge.
	Transfer *Transfer `json:"transfer,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TransferOrErr returns the Transfer value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TransferSenderEdges) TransferOrErr() (*Transfer, error) {
	if e.Transfer != nil {
		return e.Transfer, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: transfer.Label}
	}
	return nil, &NotLoadedError{edge: "transfer"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TransferSender) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case transfersender.FieldIdentityPubkey:
			values[i] = new(keys.Public)
		case transfersender.FieldTransferType:
			values[i] = new(sql.NullString)
		case transfersender.FieldCreateTime, transfersender.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case transfersender.FieldID, transfersender.FieldTransferID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TransferSender fields.
func (ts *TransferSender) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case transfersender.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ts.ID = *value
			}
		case transfersender.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				ts.CreateTime = value.Time
			}
		case transfersender.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				ts.UpdateTime = value.Time
			}
		case transfersender.FieldTransferID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field transfer_id", values[i])
			} else if value != nil {
				ts.TransferID = *value
			}
		case transfersender.FieldIdentityPubkey:
			if value, ok := values[i].(*keys.Public); !ok {
				return fmt.Errorf("unexpected type %T for field identity_pubkey", values[i])
			} else if value != nil {
				ts.IdentityPubkey = *value
			}
		case transfersender.FieldTransferType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field transfer_type", values[i])
			} else if value.Valid {
				ts.TransferType = schematype.TransferType(value.String)
			}
		default:
			ts.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TransferSender.
// This includes values selected through modifiers, order, etc.
func (ts *TransferSender) Value(name string) (ent.Value, error) {
	return ts.selectValues.Get(name)
}

// QueryTransfer queries the "transfer" edge of the TransferSender entity.
func (ts *TransferSender) QueryTransfer() *TransferQuery {
	return NewTransferSenderClient(ts.config).QueryTransfer(ts)
}

// Update returns a builder for updating this TransferSender.
// Note that you need to call TransferSender.Unwrap() before calling this method if this TransferSender
// was returned from a transaction, and the transaction was committed or rolled back.
func (ts *TransferSender) Update() *TransferSenderUpdateOne {
	return NewTransferSenderClient(ts.config).UpdateOne(ts)
}

// Unwrap unwraps the TransferSender entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ts *TransferSender) Unwrap() *TransferSender {
	_tx, ok := ts.config.driver.(*txDriver)
	if !ok {
		panic("ent: TransferSender is not a transactional entity")
	}
	ts.config.driver = _tx.drv
	return ts
}

// String implements the fmt.Stringer.
func (ts *TransferSender) String() string {
	var builder strings.Builder
	builder.WriteString("TransferSender(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ts.ID))
	builder.WriteString("create_time=")
	builder.WriteString(ts.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(ts.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("transfer_id=")
	builder.WriteString(fmt.Sprintf("%v", ts.TransferID))
	builder.WriteString(", ")
	builder.WriteString("identity_pubkey=")
	builder.WriteString(fmt.Sprintf("%v", ts.IdentityPubkey))
	builder.WriteString(", ")
	builder.WriteString("transfer_type=")
	builder.WriteString(fmt.Sprintf("%v", ts.TransferType))
	builder.WriteByte(')')
	return builder.String()
}

// TransferSenders is a parsable slice of TransferSender.
type TransferSenders []*TransferSender
