// Code generated by ent, DO NOT EDIT.

package entephemeral

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/common/keys"
	"github.com/lightsparkdev/spark/so/entephemeral/signingkeysharesecret"
)

// SigningKeyshareSecret is the model entity for the SigningKeyshareSecret schema.
type SigningKeyshareSecret struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// The time when the entity was created.
	CreateTime time.Time `json:"create_time,omitempty"`
	// The time when the entity was last updated.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// The ID of the signing keyshare this secret belongs to.
	SigningKeyshareID uuid.UUID `json:"signing_keyshare_id,omitempty"`
	// Version number of the secret; incremented on tweaks
	Version int32 `json:"version,omitempty"`
	// The secret share of the signing keyshare held by this SO.
	SecretShare  keys.Private `json:"secret_share,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SigningKeyshareSecret) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case signingkeysharesecret.FieldSecretShare:
			values[i] = new(keys.Private)
		case signingkeysharesecret.FieldVersion:
			values[i] = new(sql.NullInt64)
		case signingkeysharesecret.FieldCreateTime, signingkeysharesecret.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case signingkeysharesecret.FieldID, signingkeysharesecret.FieldSigningKeyshareID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SigningKeyshareSecret fields.
func (sks *SigningKeyshareSecret) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case signingkeysharesecret.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sks.ID = *value
			}
		case signingkeysharesecret.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				sks.CreateTime = value.Time
			}
		case signingkeysharesecret.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				sks.UpdateTime = value.Time
			}
		case signingkeysharesecret.FieldSigningKeyshareID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field signing_keyshare_id", values[i])
			} else if value != nil {
				sks.SigningKeyshareID = *value
			}
		case signingkeysharesecret.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				sks.Version = int32(value.Int64)
			}
		case signingkeysharesecret.FieldSecretShare:
			if value, ok := values[i].(*keys.Private); !ok {
				return fmt.Errorf("unexpected type %T for field secret_share", values[i])
			} else if value != nil {
				sks.SecretShare = *value
			}
		default:
			sks.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SigningKeyshareSecret.
// This includes values selected through modifiers, order, etc.
func (sks *SigningKeyshareSecret) Value(name string) (ent.Value, error) {
	return sks.selectValues.Get(name)
}

// Update returns a builder for updating this SigningKeyshareSecret.
// Note that you need to call SigningKeyshareSecret.Unwrap() before calling this method if this SigningKeyshareSecret
// was returned from a transaction, and the transaction was committed or rolled back.
func (sks *SigningKeyshareSecret) Update() *SigningKeyshareSecretUpdateOne {
	return NewSigningKeyshareSecretClient(sks.config).UpdateOne(sks)
}

// Unwrap unwraps the SigningKeyshareSecret entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sks *SigningKeyshareSecret) Unwrap() *SigningKeyshareSecret {
	_tx, ok := sks.config.driver.(*txDriver)
	if !ok {
		panic("entephemeral: SigningKeyshareSecret is not a transactional entity")
	}
	sks.config.driver = _tx.drv
	return sks
}

// String implements the fmt.Stringer.
func (sks *SigningKeyshareSecret) String() string {
	var builder strings.Builder
	builder.WriteString("SigningKeyshareSecret(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sks.ID))
	builder.WriteString("create_time=")
	builder.WriteString(sks.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(sks.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("signing_keyshare_id=")
	builder.WriteString(fmt.Sprintf("%v", sks.SigningKeyshareID))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", sks.Version))
	builder.WriteString(", ")
	builder.WriteString("secret_share=")
	builder.WriteString(fmt.Sprintf("%v", sks.SecretShare))
	builder.WriteByte(')')
	return builder.String()
}

// SigningKeyshareSecrets is a parsable slice of SigningKeyshareSecret.
type SigningKeyshareSecrets []*SigningKeyshareSecret
