// Code generated by ent, DO NOT EDIT.

package entephemeral

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/entephemeral/predicate"
	"github.com/lightsparkdev/spark/so/entephemeral/signingkeysharesecret"
)

// SigningKeyshareSecretQuery is the builder for querying SigningKeyshareSecret entities.
type SigningKeyshareSecretQuery struct {
	config
	ctx        *QueryContext
	order      []signingkeysharesecret.OrderOption
	inters     []Interceptor
	predicates []predicate.SigningKeyshareSecret
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SigningKeyshareSecretQuery builder.
func (sksq *SigningKeyshareSecretQuery) Where(ps ...predicate.SigningKeyshareSecret) *SigningKeyshareSecretQuery {
	sksq.predicates = append(sksq.predicates, ps...)
	return sksq
}

// Limit the number of records to be returned by this query.
func (sksq *SigningKeyshareSecretQuery) Limit(limit int) *SigningKeyshareSecretQuery {
	sksq.ctx.Limit = &limit
	return sksq
}

// Offset to start from.
func (sksq *SigningKeyshareSecretQuery) Offset(offset int) *SigningKeyshareSecretQuery {
	sksq.ctx.Offset = &offset
	return sksq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sksq *SigningKeyshareSecretQuery) Unique(unique bool) *SigningKeyshareSecretQuery {
	sksq.ctx.Unique = &unique
	return sksq
}

// Order specifies how the records should be ordered.
func (sksq *SigningKeyshareSecretQuery) Order(o ...signingkeysharesecret.OrderOption) *SigningKeyshareSecretQuery {
	sksq.order = append(sksq.order, o...)
	return sksq
}

// First returns the first SigningKeyshareSecret entity from the query.
// Returns a *NotFoundError when no SigningKeyshareSecret was found.
func (sksq *SigningKeyshareSecretQuery) First(ctx context.Context) (*SigningKeyshareSecret, error) {
	nodes, err := sksq.Limit(1).All(setContextOp(ctx, sksq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{signingkeysharesecret.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sksq *SigningKeyshareSecretQuery) FirstX(ctx context.Context) *SigningKeyshareSecret {
	node, err := sksq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SigningKeyshareSecret ID from the query.
// Returns a *NotFoundError when no SigningKeyshareSecret ID was found.
func (sksq *SigningKeyshareSecretQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = sksq.Limit(1).IDs(setContextOp(ctx, sksq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{signingkeysharesecret.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sksq *SigningKeyshareSecretQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := sksq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SigningKeyshareSecret entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SigningKeyshareSecret entity is found.
// Returns a *NotFoundError when no SigningKeyshareSecret entities are found.
func (sksq *SigningKeyshareSecretQuery) Only(ctx context.Context) (*SigningKeyshareSecret, error) {
	nodes, err := sksq.Limit(2).All(setContextOp(ctx, sksq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{signingkeysharesecret.Label}
	default:
		return nil, &NotSingularError{signingkeysharesecret.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sksq *SigningKeyshareSecretQuery) OnlyX(ctx context.Context) *SigningKeyshareSecret {
	node, err := sksq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SigningKeyshareSecret ID in the query.
// Returns a *NotSingularError when more than one SigningKeyshareSecret ID is found.
// Returns a *NotFoundError when no entities are found.
func (sksq *SigningKeyshareSecretQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = sksq.Limit(2).IDs(setContextOp(ctx, sksq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{signingkeysharesecret.Label}
	default:
		err = &NotSingularError{signingkeysharesecret.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sksq *SigningKeyshareSecretQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := sksq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SigningKeyshareSecrets.
func (sksq *SigningKeyshareSecretQuery) All(ctx context.Context) ([]*SigningKeyshareSecret, error) {
	ctx = setContextOp(ctx, sksq.ctx, ent.OpQueryAll)
	if err := sksq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SigningKeyshareSecret, *SigningKeyshareSecretQuery]()
	return withInterceptors[[]*SigningKeyshareSecret](ctx, sksq, qr, sksq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sksq *SigningKeyshareSecretQuery) AllX(ctx context.Context) []*SigningKeyshareSecret {
	nodes, err := sksq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SigningKeyshareSecret IDs.
func (sksq *SigningKeyshareSecretQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if sksq.ctx.Unique == nil && sksq.path != nil {
		sksq.Unique(true)
	}
	ctx = setContextOp(ctx, sksq.ctx, ent.OpQueryIDs)
	if err = sksq.Select(signingkeysharesecret.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sksq *SigningKeyshareSecretQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := sksq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sksq *SigningKeyshareSecretQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sksq.ctx, ent.OpQueryCount)
	if err := sksq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sksq, querierCount[*SigningKeyshareSecretQuery](), sksq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sksq *SigningKeyshareSecretQuery) CountX(ctx context.Context) int {
	count, err := sksq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sksq *SigningKeyshareSecretQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sksq.ctx, ent.OpQueryExist)
	switch _, err := sksq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("entephemeral: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sksq *SigningKeyshareSecretQuery) ExistX(ctx context.Context) bool {
	exist, err := sksq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SigningKeyshareSecretQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sksq *SigningKeyshareSecretQuery) Clone() *SigningKeyshareSecretQuery {
	if sksq == nil {
		return nil
	}
	return &SigningKeyshareSecretQuery{
		config:     sksq.config,
		ctx:        sksq.ctx.Clone(),
		order:      append([]signingkeysharesecret.OrderOption{}, sksq.order...),
		inters:     append([]Interceptor{}, sksq.inters...),
		predicates: append([]predicate.SigningKeyshareSecret{}, sksq.predicates...),
		// clone intermediate query.
		sql:       sksq.sql.Clone(),
		path:      sksq.path,
		modifiers: append([]func(*sql.Selector){}, sksq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SigningKeyshareSecret.Query().
//		GroupBy(signingkeysharesecret.FieldCreateTime).
//		Aggregate(entephemeral.Count()).
//		Scan(ctx, &v)
func (sksq *SigningKeyshareSecretQuery) GroupBy(field string, fields ...string) *SigningKeyshareSecretGroupBy {
	sksq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SigningKeyshareSecretGroupBy{build: sksq}
	grbuild.flds = &sksq.ctx.Fields
	grbuild.label = signingkeysharesecret.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.SigningKeyshareSecret.Query().
//		Select(signingkeysharesecret.FieldCreateTime).
//		Scan(ctx, &v)
func (sksq *SigningKeyshareSecretQuery) Select(fields ...string) *SigningKeyshareSecretSelect {
	sksq.ctx.Fields = append(sksq.ctx.Fields, fields...)
	sbuild := &SigningKeyshareSecretSelect{SigningKeyshareSecretQuery: sksq}
	sbuild.label = signingkeysharesecret.Label
	sbuild.flds, sbuild.scan = &sksq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SigningKeyshareSecretSelect configured with the given aggregations.
func (sksq *SigningKeyshareSecretQuery) Aggregate(fns ...AggregateFunc) *SigningKeyshareSecretSelect {
	return sksq.Select().Aggregate(fns...)
}

func (sksq *SigningKeyshareSecretQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sksq.inters {
		if inter == nil {
			return fmt.Errorf("entephemeral: uninitialized interceptor (forgotten import entephemeral/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sksq); err != nil {
				return err
			}
		}
	}
	for _, f := range sksq.ctx.Fields {
		if !signingkeysharesecret.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("entephemeral: invalid field %q for query", f)}
		}
	}
	if sksq.path != nil {
		prev, err := sksq.path(ctx)
		if err != nil {
			return err
		}
		sksq.sql = prev
	}
	return nil
}

func (sksq *SigningKeyshareSecretQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SigningKeyshareSecret, error) {
	var (
		nodes = []*SigningKeyshareSecret{}
		_spec = sksq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SigningKeyshareSecret).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SigningKeyshareSecret{config: sksq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(sksq.modifiers) > 0 {
		_spec.Modifiers = sksq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sksq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sksq *SigningKeyshareSecretQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sksq.querySpec()
	if len(sksq.modifiers) > 0 {
		_spec.Modifiers = sksq.modifiers
	}
	_spec.Node.Columns = sksq.ctx.Fields
	if len(sksq.ctx.Fields) > 0 {
		_spec.Unique = sksq.ctx.Unique != nil && *sksq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sksq.driver, _spec)
}

func (sksq *SigningKeyshareSecretQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(signingkeysharesecret.Table, signingkeysharesecret.Columns, sqlgraph.NewFieldSpec(signingkeysharesecret.FieldID, field.TypeUUID))
	_spec.From = sksq.sql
	if unique := sksq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sksq.path != nil {
		_spec.Unique = true
	}
	if fields := sksq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, signingkeysharesecret.FieldID)
		for i := range fields {
			if fields[i] != signingkeysharesecret.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sksq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sksq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sksq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sksq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sksq *SigningKeyshareSecretQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sksq.driver.Dialect())
	t1 := builder.Table(signingkeysharesecret.Table)
	columns := sksq.ctx.Fields
	if len(columns) == 0 {
		columns = signingkeysharesecret.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sksq.sql != nil {
		selector = sksq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sksq.ctx.Unique != nil && *sksq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range sksq.modifiers {
		m(selector)
	}
	for _, p := range sksq.predicates {
		p(selector)
	}
	for _, p := range sksq.order {
		p(selector)
	}
	if offset := sksq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sksq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (sksq *SigningKeyshareSecretQuery) ForUpdate(opts ...sql.LockOption) *SigningKeyshareSecretQuery {
	if sksq.driver.Dialect() == dialect.Postgres {
		sksq.Unique(false)
	}
	sksq.modifiers = append(sksq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return sksq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (sksq *SigningKeyshareSecretQuery) ForShare(opts ...sql.LockOption) *SigningKeyshareSecretQuery {
	if sksq.driver.Dialect() == dialect.Postgres {
		sksq.Unique(false)
	}
	sksq.modifiers = append(sksq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return sksq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sksq *SigningKeyshareSecretQuery) Modify(modifiers ...func(s *sql.Selector)) *SigningKeyshareSecretSelect {
	sksq.modifiers = append(sksq.modifiers, modifiers...)
	return sksq.Select()
}

// SigningKeyshareSecretGroupBy is the group-by builder for SigningKeyshareSecret entities.
type SigningKeyshareSecretGroupBy struct {
	selector
	build *SigningKeyshareSecretQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sksgb *SigningKeyshareSecretGroupBy) Aggregate(fns ...AggregateFunc) *SigningKeyshareSecretGroupBy {
	sksgb.fns = append(sksgb.fns, fns...)
	return sksgb
}

// Scan applies the selector query and scans the result into the given value.
func (sksgb *SigningKeyshareSecretGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sksgb.build.ctx, ent.OpQueryGroupBy)
	if err := sksgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SigningKeyshareSecretQuery, *SigningKeyshareSecretGroupBy](ctx, sksgb.build, sksgb, sksgb.build.inters, v)
}

func (sksgb *SigningKeyshareSecretGroupBy) sqlScan(ctx context.Context, root *SigningKeyshareSecretQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sksgb.fns))
	for _, fn := range sksgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sksgb.flds)+len(sksgb.fns))
		for _, f := range *sksgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sksgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sksgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SigningKeyshareSecretSelect is the builder for selecting fields of SigningKeyshareSecret entities.
type SigningKeyshareSecretSelect struct {
	*SigningKeyshareSecretQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (skss *SigningKeyshareSecretSelect) Aggregate(fns ...AggregateFunc) *SigningKeyshareSecretSelect {
	skss.fns = append(skss.fns, fns...)
	return skss
}

// Scan applies the selector query and scans the result into the given value.
func (skss *SigningKeyshareSecretSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, skss.ctx, ent.OpQuerySelect)
	if err := skss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SigningKeyshareSecretQuery, *SigningKeyshareSecretSelect](ctx, skss.SigningKeyshareSecretQuery, skss, skss.inters, v)
}

func (skss *SigningKeyshareSecretSelect) sqlScan(ctx context.Context, root *SigningKeyshareSecretQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(skss.fns))
	for _, fn := range skss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*skss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := skss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (skss *SigningKeyshareSecretSelect) Modify(modifiers ...func(s *sql.Selector)) *SigningKeyshareSecretSelect {
	skss.modifiers = append(skss.modifiers, modifiers...)
	return skss
}
